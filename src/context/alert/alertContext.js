import { createContext } from "react";

const alertContext = createContext();

export default alertContext;
//Alert Context Workflow - 1:20 After copy and pasting this, we want to copy the "GithubState.js" and opy that insto "AlertState.js" (1:42 go to AlertState.js)